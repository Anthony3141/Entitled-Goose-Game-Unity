using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProjectileCommandSenderTemp : MonoBehaviour
{
    /*
    public GameObject ProjectileMaker;
    public int spellIndex;
    private GameObject thisProjectileMaker;


    // Start is called before the first frame update
    void Start()
    {

        //only a single projectile maker should be made
        thisProjectileMaker = Instantiate(ProjectileMaker);

    }

    float timer = 0;
    float timer2 = 0.5f;

    // Update is called once per frame
    void Update()
    {
        float elapsed = Time.deltaTime;
        timer += elapsed;
        timer2 += elapsed;
        if (timer > 0.5)
        {
            //call this function every time you want to make a new spell, the input should be the spell type
            thisProjectileMaker.GetComponent<ProjectileSpawner>().spawnPlayerProjectile(0);
            timer = 0;
        }

        if (timer2 > 1)
        {
            //call this function every time you want to make a new spell, the input should be the spell type
            thisProjectileMaker.GetComponent<ProjectileSpawner>().spawnEnemyProjectile(0);
            timer2 = 0;

        }



    }

    public void SpawnPlayerProjectile()
    {

    }*/
}
